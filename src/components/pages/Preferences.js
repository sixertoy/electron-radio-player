import React from 'react';

const PreferencesWindow = () => (
  <div id="page-preferences"
    className="app-page">
    <span>this is the preferences window</span>
  </div>
);

export default PreferencesWindow;
